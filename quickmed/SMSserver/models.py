from django.db import models

# Create your models here.
class Patient(models.Model):
    """The patient model to store their information"""
    # name = get from text message
    # number = get from text message
    # diagnosis = get from medical datebase
    # location = get from SMS zip code
    
    # def __str__(self):
    """string to represent the patient name"""
    #return (self.name)
